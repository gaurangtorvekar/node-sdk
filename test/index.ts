import { Bastion } from '../src/.';

const client = new Bastion({
  apiKey: 'your-api-key',
  baseUrl: 'https://jsonplaceholder.typicode.com',
});

// const res = client.smartWallet.sampleFunc({sampleArg: "sample"})
// console.log(res);

